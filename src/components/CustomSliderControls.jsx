// import React from "react";
// import styles from "./CustomSliderControls.module.css";

// const CustomSliderControls = ({ onClick, isNext }) => {
//   return (
//     <button
//       className={`${styles.customButton} ${isNext ? styles.next : styles.prev}`}
//       onClick={onClick}
//     >
//       {isNext ? "Next" : "Prev"}
//     </button>
//   );
// };

// export default CustomSliderControls;
